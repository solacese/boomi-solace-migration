queue:
  prefix: boomi
  separator: _
  max_length: 80
  case: lower
  collision_hash_length: 8
  allowed_pattern: "^[a-z0-9_.-]+$"
topic:
  separator: /
  max_length: 250
  case: preserve
  taxonomy: "Domain/Noun/Verb/Version/Properties"
reserved_words:
  - "#DEAD_MSG_QUEUE"
  - default
